<h3>{{ 'labels.scores'|trans }} ({{ scores|length }})</h3>

<table class="wikitable">
  <tr>
    <th>{{ 'labels.game'|trans }}</th>
    <th>{{ 'labels.score'|trans }}</th>
  </tr>
{% for entry in scores %}
  {{ entry|raw }}
{% endfor %}
</table>
